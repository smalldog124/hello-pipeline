{
  "name": "tests",
  "version": "1.0.0",
  "description": "## Install cypress ```text npm install cypress ```",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^3.4.1"
  }
}
